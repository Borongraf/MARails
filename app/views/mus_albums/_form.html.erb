<%= form_with(model: mus_album) do |form| %>
  <% if mus_album.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(mus_album.errors.count, "error") %> prohibited this album from being saved:</h2>

      <ul>
        <% mus_album.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_field :description %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
